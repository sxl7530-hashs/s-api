# 测试计划 — 上游合并 + Gemini 图片尺寸倍率

> **日期**: 2026-02-15  
> **分支**: `dev`  
> **提交**: `452cbbb4` (合并上游) + `a71d2170` (Gemini 图片尺寸倍率)  
> **测试环境**: new-api-2 (端口 3002)

---

## 一、部署前准备

- [ ] 部署到测试环境 `new-api-2`（端口 3002）
- [ ] 确认数据库连接正常
- [ ] 确认服务启动无报错

---

## 二、上游合并功能测试

### 2.1 渠道类型（优先级：高）

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| Codex 渠道 (ID=57) | 后台新建 Codex 类型渠道 | 渠道类型显示为 Codex，ID=57 |
| KIE 渠道 (ID=58) | 后台新建 KIE 类型渠道 | 渠道类型显示为 KIE，ID=58 |
| 其他渠道类型 | 检查已有渠道是否正常 | 所有已有渠道不受影响 |

- [ ] 通过
- [ ] 备注：

### 2.2 Gemini 相关（优先级：高）

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| Gemini 文本对话 | 通过 API 发送 Gemini 文本请求 | 正常返回，token 计费正确 |
| Gemini 图片生成 (不指定尺寸) | 请求不带 imageSize | 正常返回，按基础价格计费 |
| Gemini 图片生成 (2K) | 请求 imageSize="2K" | 正常返回，倍率=1.0 |
| Gemini 图片生成 (4K) | 请求 imageSize="4K" | 正常返回，倍率=2.0（默认） |
| Gemini 原生接口 | 通过 /v1beta/ 路径请求 | 正常转发和计费 |
| Gemini 内容过滤 | 发送违规内容 | 返回错误，日志中有 admin_reject_reason |
| Gemini 模型列表获取 | 调用 FetchGeminiModels | 正常返回模型列表 |

- [ ] 通过
- [ ] 备注：

### 2.3 Claude 相关（优先级：高）

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| Claude 文本对话 | 普通 Claude 请求 | 正常返回 |
| Claude 流式对话 | 流式 Claude 请求 | 正常流式输出，token 计费正确 |
| Claude 语义用量标记 | 检查日志中 `usage_semantic` | 日志 other 字段包含 `"claude": true` |
| Claude Beta Query | 渠道设置 claude_beta_query | 请求正确携带 beta header |

- [ ] 通过
- [ ] 备注：

### 2.4 AWS Bedrock（优先级：中）

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| AWS 流式请求 | 通过 AWS 渠道发送请求 | 流式正常，debug 日志正确 |

- [ ] 通过
- [ ] 备注：

### 2.5 Sora 视频相关（优先级：中）

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| Sora 视频生成 | 发送 Sora 生成请求 | 正常转发到上游 |
| Sora Characters（自定义） | 访问 characters 端点 | 正常返回 |
| Sora Remix（上游新增） | 访问 remix 端点 | 正常返回 |
| 视频查询路由 | GET /v1/video/* | 所有路由正常响应 |

- [ ] 通过
- [ ] 备注：

### 2.6 Codex OAuth（上游新增，优先级：低）

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| Codex 渠道 OAuth UI | 编辑 Codex 渠道，点击 OAuth 按钮 | OAuth 弹窗正常显示 |

- [ ] 通过
- [ ] 备注：

### 2.7 日志和计费（优先级：高）

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 使用日志显示 | 查看使用日志页面 | 正常渲染，无报错 |
| Claude 日志内容 | 查看 Claude 请求的日志详情 | renderClaudeLogContent 正常显示 |
| 违规费用检查 | 如有违规费用日志 | violation fee 正确展示 |
| image_price_ratio 日志 | 使用 Gemini 4K 后查看日志 | other 字段包含 image_price_ratio |

- [ ] 通过
- [ ] 备注：

---

## 三、Gemini 图片尺寸倍率（新功能）

### 3.1 后台配置（优先级：高）

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 默认值加载 | 进入 设置→模型倍率设置 | 看到 "Gemini 图片尺寸倍率" 字段，默认 `{"1K":1.0,"2K":1.0,"4K":2.0}` |
| 修改保存 | 把 4K 改为 2.5，保存 | 保存成功，刷新后仍为 2.5 |
| 新增尺寸 | 添加 `"8K": 3.0`，保存 | 保存成功 |
| JSON 校验 | 输入非法 JSON | 显示校验错误，无法保存 |
| 重启后持久化 | 修改后重启服务 | 配置仍保持修改后的值 |

- [ ] 通过
- [ ] 备注：

### 3.2 计费验证（优先级：高）

以 `gemini-3-pro-image-preview` 为例，假设模型固定价格为 $0.03/次：

| 测试项 | 请求参数 | 预期扣费 |
|--------|----------|----------|
| 不指定尺寸 | 无 imageConfig | $0.03（无倍率） |
| 1K 尺寸 | imageSize="1K" | $0.03 × 1.0 = $0.03 |
| 2K 尺寸 | imageSize="2K" | $0.03 × 1.0 = $0.03 |
| 4K 尺寸（默认倍率） | imageSize="4K" | $0.03 × 2.0 = $0.06 |
| 修改后的 4K 倍率 | 后台改 4K=2.5 后测试 | $0.03 × 2.5 = $0.075 |

验证方式：查看使用日志中的 `image_price_ratio` 字段和实际扣费额度。

- [ ] 通过
- [ ] 备注：

### 3.3 测试用 curl 命令参考

```bash
# Gemini 图片生成（原生格式，4K）
curl -X POST "http://localhost:3002/v1beta/models/gemini-3-pro-image-preview:generateContent" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "contents": [{"parts": [{"text": "A beautiful sunset over mountains"}]}],
    "generationConfig": {
      "responseModalities": ["IMAGE"],
      "imageConfig": {"imageSize": "4K"}
    }
  }'

# 同样请求但 2K
curl -X POST "http://localhost:3002/v1beta/models/gemini-3-pro-image-preview:generateContent" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "contents": [{"parts": [{"text": "A beautiful sunset over mountains"}]}],
    "generationConfig": {
      "responseModalities": ["IMAGE"],
      "imageConfig": {"imageSize": "2K"}
    }
  }'
```

---

## 四、自定义功能回归测试

### 4.1 渠道余额监控（优先级：中）

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 余额监控页面 | 访问渠道余额监控 | 正常显示 |
| 余额刷新 | 手动触发刷新 | 正常获取余额 |
| 监控开关 | 禁用某渠道监控 | 该渠道不再自动查询 |
| 凭据保存 | 更新渠道后检查用户名/密码 | 凭据不丢失 |

- [ ] 通过
- [ ] 备注：

### 4.2 自定义图片处理（优先级：中）

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| ChatImageHandler | 通过 Gemini 渠道请求图片对话 | 正常返回图片 |
| convertToOaiImageResponse | Gemini 图片 → OpenAI 格式 | 格式转换正确 |

- [ ] 通过
- [ ] 备注：

---

## 五、基础功能冒烟测试

| 测试项 | 预期结果 |
|--------|----------|
| 登录后台 | 正常登录 |
| 渠道列表 | 正常显示所有渠道 |
| 新建渠道 | 可正常创建 |
| 编辑渠道 | 所有字段正常保存（包括 balance monitoring 和 claude_beta_query） |
| 令牌管理 | 正常 CRUD |
| 用户管理 | 正常 CRUD |
| OpenAI 兼容请求 | GPT-4o 等正常对话 |
| 语言切换 | 切换中英文正常 |

- [ ] 全部通过
- [ ] 备注：

---

## 六、测试通过后操作

1. [ ] 确认所有高优先级项通过
2. [ ] 确认无严重 bug
3. [ ] 推送到生产环境 `new-api`（端口 3000）
4. [ ] 生产环境冒烟测试

---

## 备注区

> 记录测试过程中发现的问题：
>
> -
> -
> -
